# P03 implementation evidence — 2026-08-23

## Result and claim boundary

P03 implements a complete deterministic rotary-position behavior lesson.
Static inspection establishes repository structure, source contracts, formula-oracle coverage, and bounded
interfaces. The source represents an analytic simulated lesson model, but neither the model nor its UI was
executed in MATLAB in this environment.

Evidence classification: **Static**. The independent Python oracle executes a separately written version
of the documented equations; it is static formula-contract evidence, not MATLAB runtime or executed
numerical fidelity evidence.

The real manifest changed to `implemented`/`simulated` only after an isolated promoted candidate passed
both focused and repository-wide verification.

## Acceptance mapping

| Acceptance item | Implementation and evidence | Current status |
| --- | --- | --- |
| Preserve the guiding question | Exact text is retained in the manifest, module README, `lesson.m`, `lesson.md`, and focused tests. | Pass — static |
| Visible read → baseline → one lever → changed view → mechanism explanation | Sectioned lesson and experiment; position then velocity baseline; reset-driven walkthrough; each sweep precedes its explanation. | Pass — static |
| Deterministic experiment, labels/metrics, two sweeps, broken case | Fixed analytic baseline; command and damping sweeps; five labeled figures; broken 70-degree request against 45-degree authority. | Pass — static |
| Separate model and meaningful controls | Presentation-free `model.m`; five bounded physical/design controls plus reset in `interactive.m`. | Pass — static |
| Connect to prerequisite without syntax-first teaching | P02's accepted physical effect becomes a quantitative response envelope beginning after command-path latency. | Pass — static |
| Independent invariants, limits, interpretation, teach-back | Analytic overshoot oracle; signed, zero, critical, inclusive deadline-boundary, authority, malformed-input, recovery, isolation, compatibility, and resource checks; interpretation prompts and teach-back. | Pass — static; MATLAB execution unperformed |
| Advance manifest only after verification gates | An isolated promoted candidate passed 10 focused tests, the contract verifier, and all 32 repository tests before the real P03 lifecycle fields changed. | Pass — staged lifecycle gate |
| Keep validation classes distinct | This record names every performed and unperformed validation class. | Pass — static |

## Figure, control, metric, and unit inventory

Figures and views:

- baseline position — time from accepted physical command in milliseconds versus rotary position in degrees,
  with requested position, position tolerance, and response deadline;
- baseline velocity — milliseconds versus degrees per second, with velocity tolerance and response deadline;
- command-magnitude sweep — requested degrees versus peak degrees and peak degrees per second;
- damping-ratio sweep — dimensionless damping ratio versus overshoot percent and sustained settling milliseconds;
- broken command-authority case — milliseconds versus requested, effective, and observed position in degrees.

Interactive controls:

- command angle: -75 to 75 degrees, baseline 30 degrees;
- damping ratio: 0.2 to 1.0 dimensionless, baseline 0.7;
- natural frequency: 0.5 to 3.0 hertz, baseline 1.5 hertz;
- position-command authority: 20 to 60 degrees, baseline 45 degrees;
- response deadline: 300 to 2500 milliseconds, baseline 1200 milliseconds;
- reset baseline button; fixed position tolerance 0.5 degree and velocity tolerance 2 degrees per second.

Model metrics:

- requested/effective command, authority margin, observed position, request error, and effective-target
  error — degrees;
- velocity and future velocity envelope — degrees per second;
- time, sustained settling, effective-target settling, deadline, deadline sample, sample time, and fixed
  horizon — milliseconds;
- natural frequency — hertz and radians per second; damping ratio — dimensionless;
- peak position, peak velocity, overshoot degrees and percent, final error, future position envelope;
- command-limited, direction-correct, deadline-met, future-guaranteed, and complete-requirement flags;
- failure mode — `none`, `command-limited`, `direction-error`, or `deadline-missed`;
- fixed trace size — 5001 samples at 2 milliseconds over 10000 milliseconds; maximum accepted deadline
  3000 milliseconds.

## Exact commands and results

Environment inventory:

- branch: `agent/p03`;
- starting HEAD: `d0188d9c3c17badffb7b1d272ad42ac4977536e4`;
- Python: 3.11.2;
- MATLAB: not found on `PATH`;
- Octave: not found on `PATH`;
- worktrees: this isolated `agent/p03` worktree plus `/home/kbianco/src/hwil-systems-learning` on local `main`;
- pre-existing change: governed `contracts/active-batch.yaml` selects P03 and is preserved as an operator/control-plane change.

| Command | Exit | Result |
| --- | ---: | --- |
| `MATLAB_LEARNING_VERIFY_PROFILE=contract PYTHONDONTWRITEBYTECODE=1 python3 scripts/verify.py` (pre-change baseline) | 0 | `VERIFY PASS: 24 modules` |
| `MATLAB_LEARNING_VERIFY_PROFILE=quick PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v` (pre-change baseline) | 0 | 22 tests passed in 0.628 s |
| `MATLAB_LEARNING_VERIFY_PROFILE=full PYTHONDONTWRITEBYTECODE=1 ./scripts/agent-verify.sh` (pre-change baseline) | 0 | verifier and 22 tests passed; ignored log `docs/evidence/local/verify-20260823-062759.log` |
| Focused pre-promotion P03 source test selection, excluding lifecycle/evidence assertions | 0 | 8 tests passed in 0.065 s |
| `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=tests python3 -m unittest -v test_p03_module` in isolated promoted candidate `/tmp/hwil-p03-stage.AZxf6Q/repo` | 0 | 10 tests passed in 0.047 s |
| `MATLAB_LEARNING_VERIFY_PROFILE=contract PYTHONDONTWRITEBYTECODE=1 python3 scripts/verify.py` in isolated promoted candidate | 0 | `VERIFY PASS: 24 modules` |
| `MATLAB_LEARNING_VERIFY_PROFILE=full PYTHONDONTWRITEBYTECODE=1 ./scripts/agent-verify.sh` in isolated promoted candidate | 0 | verifier and 32 tests passed in 0.896 s; candidate log `docs/evidence/local/verify-20260823-064758.log` inside the isolated copy |
| `./bin/learn status && ./bin/learn start P03` after promotion | 0 | reported 24 total/3 implemented and selected P03 with its exact guiding question; the created ignored progress file was removed after the smoke check |
| `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=tests python3 -m unittest -v test_p03_module` after promotion | 0 | 10 tests passed in 0.047 s |
| `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=tests python3 -m unittest -v test_learn_cli` after promotion | 0 | 6 tests passed in 0.769 s, including empty-state compatibility, refusal isolation, unknown-current recovery, and rollback recovery |
| `MATLAB_LEARNING_VERIFY_PROFILE=contract PYTHONDONTWRITEBYTECODE=1 python3 scripts/verify.py` after promotion | 0 | `VERIFY PASS: 24 modules` |
| `MATLAB_LEARNING_VERIFY_PROFILE=quick PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v` after promotion | 0 | 32 tests passed in 0.820 s |
| `MATLAB_LEARNING_VERIFY_PROFILE=full PYTHONDONTWRITEBYTECODE=1 ./scripts/agent-verify.sh` after promotion | 0 | `VERIFY PASS: 24 modules`; 32 tests passed in 0.798 s; ignored log `docs/evidence/local/verify-20260823-064902.log` |
| `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=tests python3 -m unittest -v test_p03_module` after independent audit repairs | 0 | 10 tests passed in 0.079 s, including executed Python regressions for finite-horizon and slow zero-input limits |
| `MATLAB_LEARNING_VERIFY_PROFILE=full PYTHONDONTWRITEBYTECODE=1 ./scripts/agent-verify.sh` after independent audit repairs | 0 | `VERIFY PASS: 24 modules`; 32 tests passed in 0.781 s; ignored log `docs/evidence/local/verify-20260823-065520.log` |
| `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=tests python3 -m unittest -v test_p03_module` after deadline-boundary review repair | 0 | 11 tests passed in 0.114 s; exact 780 ms tie passed, 779.999 ms deadline failed, and non-grid sampling did not round forward |
| `MATLAB_LEARNING_VERIFY_PROFILE=contract PYTHONDONTWRITEBYTECODE=1 python3 scripts/verify.py` after deadline-boundary review repair | 0 | `VERIFY PASS: 24 modules` |
| `MATLAB_LEARNING_VERIFY_PROFILE=quick PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v` after deadline-boundary review repair | 0 | 33 tests passed in 0.817 s |
| `MATLAB_LEARNING_VERIFY_PROFILE=full PYTHONDONTWRITEBYTECODE=1 ./scripts/agent-verify.sh` after deadline-boundary review repair | 0 | `VERIFY PASS: 24 modules`; 33 tests passed in 0.765 s; ignored log `docs/evidence/local/verify-20260823-070656.log` |

Observed CI state before editing:

- Portfolio Control `Control plane validation` run `32609715164` passed for governing revision
  `4434b282cfb47997d791bd2933faf980453d199a` on 2026-08-23.
- Target `Quality` run `32622664219` passed for `main` commit
  `d0188d9c3c17badffb7b1d272ad42ac4977536e4` on 2026-08-23.
- `agent/p03` is local and unpushed, so neither observed CI run validates this P03 working tree.

## Profile applicability

- Schema, canonical-data, uniqueness, and completeness gates apply to the 24-module manifest and artifact
  state; repository verification covers them statically.
- The repository has no scoring engine, database, migration, backup/restore surface, or service for P03.
  Scoring-invariant, data-migration, backup-restore, and service-smoke gates are not applicable to this
  bounded source lesson.
- Response deadline miss is P03's timeout analogue. Transaction cancellation remains owned and tested by
  prerequisite P02 rather than being duplicated as a plant-response input.
- Rollback is a source and manifest operation; the procedure is specified below. The shared CLI suite
  covers recovery when a persisted current module is rolled back to a scaffolded state.
- Dataset count: 24 canonical modules; P03 owns one complete nine-file module slice.

## Changed invariants

- P03 lifecycle changes from `scaffolded`/`none` to `implemented`/`simulated` after its staged gates pass.
- Implemented modules form the contiguous P01–P03 prefix at this batch point; shared tests derive the frontier so later batches may advance it.
- P03 defines time zero as accepted physical command time and does not double-count P02 command or feedback latency.
- A successful response requires a valid signed command plus sustained position and velocity bands at or before the deadline.
- The response-deadline comparison is inclusive: settling at 780 milliseconds passes, any earlier deadline
  fails, and a non-grid deadline reports the latest two-millisecond sample at or before it.
- A finite trace may provide last-exit timing only when an analytic decay envelope guarantees future tolerance compliance.
- Zero input from zero initial state is settled immediately even when the configured response rate is extremely slow.
- A command-authority violation is reported distinctly even if the response settles around its effective target.

## Preserved invariants

- The manifest retains exactly 24 unique modules in canonical order and six phases.
- P03 retains number, ID, title, exact guiding question, phase, slug, folder, batch, and P02 prerequisite.
- P04 and later identities and owned folders remain unchanged; no P03 test requires their status to stay
  scaffolded or calls P03 permanently latest.
- The model has fixed-size outputs, no random state, no persistent/global state, no external I/O, no opaque
  toolbox call, and no presentation operation.
- Learner progress remains ignored under `.learning/`; forbidden paths and the pre-existing active-batch
  selection remain unchanged by implementation work.

## Independent system-risk review repair

- Finding: the inclusive response-deadline boundary was named in lesson text and unexecuted MATLAB checks,
  but the available Python gate only searched for source markers and did not execute the boundary behavior.
- Repair: `tests/test_p03_module.py` now executes the independent oracle at 780, 779.999, and 781
  milliseconds, verifies inclusive pass/fail behavior, verifies floor-to-sample observables, proves the
  deadline does not alter the trajectory, and binds those expectations to the retained MATLAB regression.
- Scope: no production model, public interface, contract, or forbidden-path content changed during this
  repair. The governed `contracts/active-batch.yaml` selector present at review start remains untouched.

## Residual risks

- MATLAB syntax, analytic function behavior, graphics, UI callbacks, and control lifetime remain unexecuted
  until a named licensed MATLAB environment runs the lesson and checks.
- Static formula inspection and an independent Python oracle do not prove MATLAB-executed numerical fidelity.
- The analytic model is an instructional zero-initial-state second-order response, not an identified actuator,
  torque-saturation, hard-stop, structural-load, or sensor model.
- Educational clarity and learner interpretation require a future guided session and teach-back.

## Rollback

Revert only P03-owned module files, `tests/test_p03_module.py`, this P03 evidence file, allowed shared
documentation changes, and restore only P03 `status` to `scaffolded` with `evidence_level` to `none`.
The shared rollback-recovery CLI test remains the non-destructive compatibility check. Destructive source
rollback is not performed while validating the implementation candidate.

## Unperformed validation

- licensed MATLAB execution of `experiment.m` and `run_checks.m`;
- interactive UI rendering, control manipulation, callback lifetime, and manual plot review;
- executed numerical-fidelity comparison in MATLAB or an approved independent runtime;
- learner session, interpretation answers, and teach-back;
- bench, physical hardware, hardware-in-the-loop (HIL), field, RT1/RT2, and Unreal validation;
- protocol, staging, signing, packaging, release, deployment, and production validation;
- Portfolio Control validation and target Quality CI for an unpushed branch;
- destructive rollback rehearsal.
