# P11 retained evidence — 2026-08-23

## Scope and evidence boundary

Batch P11 implements **Budget Latency and Jitter** on branch `agent/p11`. The implementation answers the
canonical guiding question: “What inputs, observable effects, and failure modes matter when you budget Latency
and Jitter?”

This record supports source-level **Static** validation and deterministic, independently modeled
**simulated** behavior only. A separate Python oracle was executed against the stated equations and fixtures.
The MATLAB source was not executed because neither MATLAB nor Octave is installed in the named environment.
Static inspection and a model in another language do not constitute MATLAB runtime, UI, plot rendering, or
MATLAB-versus-oracle numerical fidelity evidence.

## Acceptance mapping

| Acceptance item | Implementation evidence | Validation evidence |
| --- | --- | --- |
| Preserve the guiding question | P11 README, MATLAB lesson, tutor lesson, checks, and manifest retain the exact canonical question | Focused identity test pins number, ID, title, question, phase, folder, batch, and prerequisite |
| Follow read → baseline → one lever → changed view → mechanism-first explanation | `lesson.m`, ordered `experiment.m` sections, tutor lesson, and walkthrough show one prediction, two baseline views, one isolated lever at a time, then the explanation | Focused source test pins section order, one launch-path prediction, absence of `clc`, held inputs, and explanation placement |
| Deterministic baseline, labeled plots/metrics, two sweeps, and one broken case | `experiment.m` contains a fixed 12-cycle baseline, cumulative stage view, transport sweep, jitter-scale sweep, event cases, and deliberately broken RSS assessment | Python oracle pins the exact baseline trace, both sweeps, limiting cases, and aligned-jitter false approval; source tests pin figure count, axes, metrics, and units |
| Separate model and presentation; expose meaningful controls | `model.m` owns bounded calculations only; `interactive.m` owns plots, seven input controls, and reset | Focused tests prohibit presentation, RNG, timers, solvers, Simulink, external I/O, global/persistent state, and opaque toolbox operations in the model; UI source checks pin bounds and callbacks |
| Connect to prerequisites without syntax-first teaching | P11 consumes a P10 activation-proof adapter, requests but does not claim a P10 SAFE-HOLD/rollback handoff, distinguishes P01 sampling, and preserves P12 synchronization | Compatibility and tutor-source tests pin the P10 facts and P01/P10/P12 boundaries without invoking or reimplementing another module |
| Independent invariants, limits, interpretation, and teach-back | `run_checks.m` independently reconstructs the pattern, equations, traces, limits, events, malformed cases, bounds, and recovery; `checks.md` contains interpretation prompts and a two-sentence teach-back | Focused Python oracle independently executes positive, negative, malformed, cancellation, timeout, neighboring and exact tie, recovery, isolation, compatibility, rollback-boundary, and resource cases |
| Manifest changes only after focused and repository-wide verification | The live manifest remained scaffolded during implementation; a complete temporary candidate was promoted and checked before the same two P11 fields changed live | Candidate-before-live and live gate commands/results are retained below |
| Distinguish validation classes | This record separates performed static/simulated checks from every unperformed runtime and physical class | Claim boundary, runtime inventory, residual risk, and unperformed validation are explicit |

## Exact commands and results

### Environment, ownership, concurrency, and CI inspection before editing

- `git branch --show-current` — exit 0; `agent/p11`.
- `git rev-parse HEAD` and recent history inspection — exit 0; starting target `db3d1cc6ebfebbb0c00bd817f8169aac385f13d5`, the merged P10 commit and exact `origin/main` at inspection time.
- `git status --short --branch` — exit 0; only the operator/control-plane P11 activation in forbidden `contracts/active-batch.yaml` was pre-existing.
- `git worktree list --porcelain`, Git lock inspection, and process inspection — exit 0; two worktrees were present, no index lock was present, and no separate MATLAB, Octave, or verifier process was running.
- Ownership inspection — exit 0; no CODEOWNERS file exists. Portfolio Control owns intent/order/scope; the target owns module content, curriculum state, tests, and retained evidence.
- `portfolio status hwil-systems-learning` — exit 0; P11 was the next software batch, target PR count was zero, and the stop gate was `none`.
- `gh run list --repo kpbianco/hwil-systems-learning ...` — exit 0; target Quality run `32644492648` passed on exact starting target `db3d1cc`.
- `gh run list --repo kpbianco/portfolio-control ...` — exit 0; Control plane validation run `32635535352` passed on the batch-pinned control revision `acdbdc50`.
- `gh pr status --repo kpbianco/hwil-systems-learning` — exit 0; no pull request existed for the current target branch.
- `command -v matlab` and `command -v octave` — exit 1 for each; neither runtime was installed.
- `sha256sum contracts/active-batch.yaml` — exit 0; pre-existing forbidden file hash `24a047dfecc7fcaa768fa486e5c6c0c09cab1302a68c6455b6ceab4e78999eb9`.

### Pre-edit baseline

- `MATLAB_LEARNING_VERIFY_PROFILE=contract python3 scripts/verify.py` — exit 0; `VERIFY PASS: 24 modules`.
- `MATLAB_LEARNING_VERIFY_PROFILE=quick PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v` — exit 0; 151 tests passed.
- `MATLAB_LEARNING_VERIFY_PROFILE=full PYTHONDONTWRITEBYTECODE=1 ./scripts/agent-verify.sh` — exit 0; verifier and 151 tests passed; ignored local log `docs/evidence/local/verify-20260823-141854.log`.

### Implementation-stage focused checks while the live manifest remained scaffolded

- `PYTHONDONTWRITEBYTECODE=1 python3 -m py_compile tests/test_p11_module.py` — exit 0.
- `git diff --check -- modules/11-budget-latency-and-jitter tests/test_p11_module.py` — exit 0.
- `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=tests python3 -m unittest -v test_p11_module` after the initial oracle implementation — exit 1; 15 of 21 tests passed, three floating-point comparisons were repaired to use tolerances, and the three intended manifest/CLI/evidence lifecycle gates remained.
- The same focused command after tolerance repair — exit 1; 18 of 21 tests passed, with only intentionally deferred manifest identity, CLI start, and retained-evidence lifecycle gates remaining.

### Candidate-before-live gates

The complete candidate used the temporary repository copy `/tmp/hwil-p11-stage.l6KXsl`. Candidate-manifest
inspection proved that only P11 `status: scaffolded → implemented` and
`evidence_level: none → simulated` differed from the live unpromoted manifest.

- Candidate/live manifest comparison before promotion — exit 1 as expected; the structured diff contained
  exactly the two P11 lifecycle fields above and no other module or manifest change.
- `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=tests python3 -m unittest -v test_p11_module` in the candidate — exit 0;
  all 21 focused P11 tests passed.
- `MATLAB_LEARNING_VERIFY_PROFILE=contract python3 scripts/verify.py` in the candidate — exit 0;
  `VERIFY PASS: 24 modules`.
- `MATLAB_LEARNING_VERIFY_PROFILE=quick PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v`
  in the candidate — exit 0; all 172 repository tests passed.
- `MATLAB_LEARNING_VERIFY_PROFILE=full PYTHONDONTWRITEBYTECODE=1 ./scripts/agent-verify.sh` in the candidate —
  exit 0; verifier and all 172 tests passed; candidate log
  `/tmp/hwil-p11-stage.l6KXsl/docs/evidence/local/verify-20260823-144403.log`.

### Live promotion and repository-wide gates

Only after the candidate gates passed were the same two P11 fields promoted in the live manifest.

- `cmp -s curriculum/modules.json /tmp/hwil-p11-stage.l6KXsl/curriculum/modules.json` — exit 0; the promoted live
  manifest exactly matched the verified candidate manifest.
- `./bin/learn status` — exit 0; 24 modules and 11 implemented modules were reported.
- `./bin/learn start P11` — exit 0; the CLI selected P11 and printed its exact canonical title and guiding
  question. The smoke-created empty learner progress was recoverably moved to
  `/tmp/hwil-p11-stage.l6KXsl/live-smoke-progress.json`, and the generated empty `.learning/` directory was
  removed after inspection.
- `./bin/learn check P11` — exit 0; P11's checks and teach-back route were printed.
- `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=tests python3 -m unittest -v test_p11_module` — exit 0; all 21 focused
  P11 tests passed.
- `MATLAB_LEARNING_VERIFY_PROFILE=contract python3 scripts/verify.py` — exit 0;
  `VERIFY PASS: 24 modules`.
- `MATLAB_LEARNING_VERIFY_PROFILE=quick PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v` —
  exit 0; all 172 repository tests passed.
- `MATLAB_LEARNING_VERIFY_PROFILE=full PYTHONDONTWRITEBYTECODE=1 ./scripts/agent-verify.sh` — exit 0; verifier
  and all 172 tests passed; ignored local log `docs/evidence/local/verify-20260823-144505.log`.

### Independent system-risk audit and repaired-tree gates

A bounded read-only post-implementation audit found no high-severity model, timing, RSS, timeout, scope, or
evidence issue. It found one medium compatibility ambiguity and two low coverage gaps:

- the documented P10 adapter applied scalar `&&` operators to P10's vector-valued transition facts instead of
  selecting the nominal `activate-request` at step 6;
- cancellation precedence at an exact planned-completion tie was documented and implemented but lacked a
  behavioral regression;
- the MATLAB checker did not independently pin the values and availability masks for actual completion and
  actual stage-completion evidence.

The adapter now explicitly indexes P10 step 6, and the focused compatibility test independently confirms that
P10's sixth nominal event is `activate-request`. Cancellation at the exact 9.5 ms cycle-2 completion now has
MATLAB experiment/check and independent Python-oracle coverage. `run_checks.m` now pins actual release,
latency, completion, stage-latency, and stage-completion masks and equality to the corresponding planned facts.
A read-only follow-up audit confirmed all three findings closed and found no additional issue.

- `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=tests python3 -m unittest -v test_p11_module` after repair — exit 0;
  all 21 focused P11 tests passed.
- `MATLAB_LEARNING_VERIFY_PROFILE=contract PYTHONDONTWRITEBYTECODE=1 python3 scripts/verify.py` after repair —
  exit 0; `VERIFY PASS: 24 modules`.
- `MATLAB_LEARNING_VERIFY_PROFILE=quick PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v`
  after repair — exit 0; all 172 repository tests passed.
- `MATLAB_LEARNING_VERIFY_PROFILE=full PYTHONDONTWRITEBYTECODE=1 ./scripts/agent-verify.sh` after repair — exit 0;
  verifier and all 172 tests passed; ignored local log
  `docs/evidence/local/verify-20260823-145356.log`.
- Final evidence-bearing rerun of the focused, contract, quick, and full commands above — exit 0 for all four;
  21 focused tests and 172 repository tests passed; final pre-record log
  `docs/evidence/local/verify-20260823-145506.log`.
- `git diff --check` — exit 0.
- Allowed-path and exact-terminal-newline audit over every tracked and untracked changed text file — exit 0;
  all implementation changes are allowed, all files end in exactly one newline, and the only forbidden-path
  diff is the pre-existing control-plane activation.
- `sha256sum contracts/active-batch.yaml` after repair — exit 0; unchanged at
  `24a047dfecc7fcaa768fa486e5c6c0c09cab1302a68c6455b6ceab4e78999eb9`.

### Independent follow-up review and oracle-boundary repair

A second independent system-risk review found one low-severity but concrete retained-evidence defect. The
Python oracle called `math.isclose` with `abs_tol=1e-12` but left Python's default `rel_tol=1e-9` enabled. At
the 22.2 ms deadline, that relative window could classify a nearby pre-deadline cancellation as both
cancellation and timeout even though the MATLAB model uses only an absolute 1e-12 ms tie tolerance. The old
exact-tie test could not distinguish those semantics. The MATLAB model itself did not require repair.

The oracle now sets `rel_tol=0.0`. A new executed behavioral regression checks cancellation 1e-9 ms before
the deadline, at the exact deadline, and 1e-9 ms after it; the expected outcomes are cancellation only, an
exact cancellation/timeout tie resolved to cancellation, and timeout only. `run_checks.m` retains the same
neighboring-boundary cases for a future MATLAB-runtime gate.

- Direct tolerance probe — exit 0; Python's former default-relative comparison returned `True` for
  `22.2 - 1e-9` versus `22.2`, while the repaired absolute-only comparison returned `False`.
- `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=tests python3 -m unittest -v test_p11_module` — exit 0; all 22 focused
  P11 tests passed, including the new neighboring-boundary regression.
- `MATLAB_LEARNING_VERIFY_PROFILE=contract PYTHONDONTWRITEBYTECODE=1 python3 scripts/verify.py` — exit 0;
  `VERIFY PASS: 24 modules`.
- `MATLAB_LEARNING_VERIFY_PROFILE=quick PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v` —
  exit 0; all 173 repository tests passed.
- `MATLAB_LEARNING_VERIFY_PROFILE=full PYTHONDONTWRITEBYTECODE=1 ./scripts/agent-verify.sh` — exit 0; verifier
  and all 173 tests passed; ignored local log
  `docs/evidence/local/verify-20260823-150520.log`.
- No MATLAB or Octave executable was available, so the new retained MATLAB checks remain unexecuted static
  evidence and no MATLAB numerical-fidelity claim is made.

## Figure, control, metric, and unit inventory

Figures in `experiment.m`:

1. Baseline scheduled latency: offline and completed-cycle end-to-end latency versus scheduled cycle, with the elapsed deadline.
2. Baseline cumulative envelope: cumulative nominal, bounded latest, and bounded earliest elapsed time versus owned stage.
3. Transport sweep: worst-case deadline margin versus nominal command-transport latency.
4. Jitter sweep: worst-case deadline margin versus dimensionless jitter-allocation scale.
5. Broken RSS assessment: reported RSS upper bound, strict additive bound, aligned planned cycle, and deadline.

Cancellation, timeout, tie, proof-blocking, recovery, and handoff cases do not create additional figures. Their
facts are printed/asserted and retained as model outputs; no MATLAB-rendered view is claimed.

Interactive inputs/actions in `interactive.m`:

- transport nominal spinner, 0.6–2.4 ms;
- jitter-scale spinner, 0–2 dimensionless;
- deadline spinner, 0.5–10 ms;
- P10 activation-proof checkbox;
- cancellation-enable checkbox;
- cancellation-time spinner, 0–72 ms;
- bounded-sum/broken-RSS assessment dropdown;
- reset-to-baseline action.

Retained metrics include per-stage nominal latency and jitter allocation, stage variation and cumulative
completion, cycle release/deadline/planned completion, planned and completed latency, nominal total, additive
and RSS allowances, lower/upper bounds, strict/reported margin and approval, peak-to-peak jitter, cycle
start/completion masks, terminal time/cycle, handoff request, rollback boundary, terminal/failure, and false
approval.

Units are explicit: latency, variation, allocation, release, completion, deadline, cancellation time, terminal
time, and margin use milliseconds (`ms`); cycle and stage are counts/indices; jitter scale, pattern, Boolean
evidence, and acceptance are dimensionless (`-`). P11 uses one ideal time base and makes no P12 distributed-clock
claim.

## Runtime inventory

- Executed: Python 3 standard library, Bash repository verifier, Git inspection, GitHub CLI read-only CI/PR
  inspection, local Portfolio Control status, static source assertions, isolated CLI fixtures, and the
  independent Python timing oracle.
- Available but not used as evidence: no alternative numerical runtime was present.
- Unavailable: MATLAB and Octave.
- No UI callback, figure rendering, MATLAB parser, hardware interface, network protocol peer, or device runtime
  was exercised.

## Resource envelope and profile applicability

The deterministic model is fixed at 12 cycles, four stages, 48 stage cells, one 6 ms ideal cycle period, and
one finite `12 x 4` pattern. Inputs are bounded; arrays are fixed; the model performs no random sampling,
dynamic event growth, solver iteration, timer wait, external I/O, or state retention.

The `product-data` profile applies as follows:

- Dataset counts: the canonical manifest retains exactly 24 unique modules; after promotion, P01–P11 form the
  contiguous implemented prefix. The P11 fixture retains exactly 12 cycles, four stages, and 48 stage cells.
- Schema/canonical-data/uniqueness/completeness gates: repository verification and curriculum tests validate the
  24 identities, ordering, prerequisites, complete P11 artifact set, and contiguous frontier.
- Scoring invariants: the repository defines no learner score. P11 timing truth, assessment truth, and false
  approval are covered by independent invariant tests instead.
- Migration and rollback: promotion changes only two existing P11 manifest fields. An isolated rollback fixture
  restores P11 and every later module to unavailable state, resumes a persisted learner at P10, derives counts
  from that fixture, and retains dormant P11 completion and notes.
- Backup/restore smoke: no database, mutable canonical dataset, or backup format exists. The isolated manifest
  and learner-state fixture exercises the applicable copy/restore behavior without a production data write.
- Service smoke: no service exists. Isolated `start P11`, `check P11`, `continue`, `status`, and `list` commands
  exercise the applicable learner CLI surface.

No destructive data migration, production data write, service mutation, or environment-setting change was
performed.

## Changed invariants

- The contiguous implemented curriculum frontier advances from P10 through P11 only after candidate
  verification.
- P11 owns a complete nine-artifact learner slice plus focused durable tests and this retained evidence record.
- The P11 public calculation is a deterministic 12-cycle, four-stage elapsed-time contract with explicit
  nominal latency, bounded jitter, absolute deadline, cancellation ordering, derived timeout, evidence masking,
  handoff authority, assessment truth, and false approval.
- The learner CLI resolves P11 as runnable after manifest promotion.

## Preserved invariants

- P11 module number, ID, title, exact guiding question, phase, slug, folder, implementation batch, and P10
  prerequisite are unchanged.
- P01 retains seeded Gaussian/P99 sampling; P11 does not call its finite fixture probabilistic evidence.
- P10 retains state legality, guard, priority, destination, SAFE-HOLD, and rollback authority. P11 consumes one
  Boolean activation-proof adapter without invoking P10 and requests rather than claims a handoff.
- P12 retains clock offset, drift, skew, timestamp alignment, and distributed synchronization. P11 uses one ideal
  time base only.
- Additive bounded assessment remains separate from the deliberately broken RSS report; the reporting rule
  cannot change the factual planned or actual schedule.
- Unfinished and post-terminal actual facts remain unavailable while the offline plan remains inspectable.
- P11 tests assert permanent P11/P10 facts but do not require any later module to remain scaffolded, do not pin
  a fixed implemented count, and do not call P11 permanently latest. Shared tests derive the moving frontier.
- Forbidden files are preserved, including the pre-existing operator activation in
  `contracts/active-batch.yaml`.
- No commit, push, pull request, release, deployment, or settings change is made.

## Residual risks

- MATLAB syntax, implicit expansion, function execution, graphics, and UI behavior were not executed in this
  environment. Static tests and an independent Python oracle cannot remove that runtime risk.
- UI layout, callback behavior, plot rendering, accessibility, and learner usability need a compatible MATLAB
  desktop session and human review.
- The fixture deliberately has one ideal time base, a fixed 12-cycle pattern, and fixed event ordering. It does
  not establish probability, workload representativeness, scheduler behavior, clock synchronization, protocol
  timing, or hardware timing fidelity.
- The P10 activation proof is caller supplied. An operational design needs authenticated, fresh evidence and a
  separately verified supervisor/handoff implementation.
- RSS is intentionally unsafe without independence evidence and exists only as a labeled broken assessment.

## Rollback

Revert only P11-owned module files, P11 focused tests, shared P11 learner references, and this evidence record;
restore only P11 `status` to `scaffolded` and `evidence_level` to `none`. The isolated rollback fixture proves
that a learner currently selecting P11 recovers to P10 while dormant P11 completion and notes remain retained.
Do not revert or rewrite the operator/control-plane activation in forbidden `contracts/active-batch.yaml`.

## Unperformed validation

No MATLAB runtime execution was performed. No interactive UI session or plot rendering was performed. No
MATLAB-versus-oracle numerical fidelity comparison was performed. No post-change CI run was performed because
the user prohibited commit, push, and pull-request actions. No protocol, bench, physical hardware/HIL, field,
RT1/RT2, Unreal, signing, release, deployment, staging, or production validation was performed. No personnel-
safety claim is made. The retained evidence level is simulated, not physical or operational.
